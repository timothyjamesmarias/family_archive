<div class="flex flex-wrap items-center gap-2.5">
  <% ArtifactType::ALL.each do |type| %>
    <%= render Ui::TypeChipComponent.new(artifact_type: type) %>
  <% end %>
</div>
<div class="flex flex-wrap items-center gap-2.5">
  <% ArtifactType::ALL.each do |type| %>
    <%= render Ui::TypeChipComponent.new(artifact_type: type, size: :sm) %>
  <% end %>
</div>
