<% flash.each do |type, message| %>
  <%= render Ui::MessageComponent.new(
        level: type.to_sym == :alert ? :error : :success,
        class: "mb-4",
        data: { testid: "flash-#{type}" }
      ).with_content(message) %>
<% end %>
